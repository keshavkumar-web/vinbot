# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-test.txt
#
# Newman (Postman collection runner) is a separate Node.js tool, not a Python
# package — see postman/package.json. There is no pip package that makes
# pytest "Newman-compatible"; the two suites are run independently and their
# reports both land under ../reports/ (see tests/README.md).

pytest>=8.0
pytest-cov>=5.0
pytest-html>=4.1
pytest-mock>=3.14
pytest-xdist>=3.6
httpx>=0.27
